{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch gomono",
      "type": "go",
      "request": "launch",
      "mode": "auto",
      "program": "${workspaceFolder}/main.go",
      "args": [
        "-i", "${workspaceFolder}/internal/deps/gomod.go",
        "-v",
      ]
    }
  ]
}